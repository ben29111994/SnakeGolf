using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class BodyPath : MonoBehaviour
{
    //private void OnTriggerEnter(Collider other)
    //{
    //    if (other.CompareTag("GroundNormal"))
    //    {
    //        if(GameManager.Instance.player.ball.isFly && GameManager.Instance.isGroundFinal == false)
    //        GameManager.Instance.EffectBodyPath(gameObject);
    //    }
    //}
}
